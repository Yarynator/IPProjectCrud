<table class="table table-striped">
    <thead>
    <tr>
        <th>Name</th>
        <th>Surname</th>
        <th>Job</th>
        <th>Wage</th>
        <th>Room</th>
    </tr>
    </thead>

    <tbody>
    {{# employees }}
        <tr>
            <td><a href="employee.php?employeeId={{ employee_id }}">{{ name }}</a></td>
            <td>{{ surname }}</td>
            <td>{{ job }}</td>
            <td>{{ wage }}</td>
            <td><a href="../rooms/room.php?roomId={{ room_id }}">{{ room }}</a></td>
            <td>
                <form class="mt-2" method="post" action="update.php">
                    <input type="hidden" name="employee_id" value="{{employee_id}}" />
                    <input type="submit" value="Editovat" class="btn btn-info" {{^admining}}disabled{{/admining}} />
                </form>
            </td>
        </tr>
    {{/ employees }}
    </tbody>
</table>
<a href="./insert.php" class="btn btn-primary mb-2">Přidat osobu</a>
